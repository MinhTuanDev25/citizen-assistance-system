# Local defaults — OK to commit. Override with env if needed.
server:
  addr: ":8080"
  shutdown_timeout_sec: 10

database:
  url: "postgres://cas:cas@localhost:5432/citizen_assistance?sslmode=disable"
  max_conns: 10
  min_conns: 1
  conn_timeout_sec: 5

app:
  xa_id: xa_chu_se
  log_level: debug

auth:
  # Local only — override with JWT_SECRET in prod.
  jwt_secret: "9f8e7d6c5b4a3f2e1d0c9b8a7f6e5d4c3b2a1f0e9d8c7b6a5f4e3d2c1b0a9f8e"
  jwt_expire_hours: 24
