# Copy to deploy/.env (never commit real secrets)

POSTGRES_USER=cas
POSTGRES_PASSWORD=cas
POSTGRES_DB=citizen_assistance
POSTGRES_PORT=5432

# Host tools (DBeaver, Go api on host)
DATABASE_URL=postgres://cas:cas@localhost:5432/citizen_assistance?sslmode=disable

XA_ID=xa_chu_se

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=admin-documents

LLM_PROVIDER=mock
# OPENAI_API_KEY=
# GEMINI_API_KEY=

API_ADDR=:8080
AI_SERVICE_URL=http://localhost:8001
JWT_SECRET=change-me-in-production
