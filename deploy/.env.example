# Copy to deploy/.env and adjust (never commit real secrets)

XA_ID=xa_demo_001
DATABASE_URL=postgres://cas:cas@localhost:5432/citizen_assistance?sslmode=disable

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=admin-documents

# LLM (leave empty to use mock extractors in Phase 1)
LLM_PROVIDER=mock
# OPENAI_API_KEY=
# GEMINI_API_KEY=

API_ADDR=:8080
AI_SERVICE_URL=http://localhost:8001
JWT_SECRET=change-me-in-production
