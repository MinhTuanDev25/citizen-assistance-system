# Shared contracts

Place OpenAPI specs and shared JSON schemas here (e.g. procedure_definition schema copies,
chat turn request/response DTOs) so Go / Python / Web stay aligned.

Seed procedure JSON currently lives in `docs/phase-0/seeds/`.
